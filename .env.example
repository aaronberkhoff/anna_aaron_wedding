# Copy this file to .env for local development.
# .env is gitignored and must never be committed.

# ---- Server ----
# SQLite database file path (relative or absolute)
DATABASE_URL=sqlite://wedding.db

# Directory where Trunk builds the frontend dist output
DIST_DIR=site/dist

# Server bind address
BIND_ADDR=0.0.0.0:8080

# Log level: error | warn | info | debug | trace
RUST_LOG=debug

# ---- sqlx ----
# Set to true to use cached .sqlx query metadata without a live DB.
# Required in CI and Docker builds. Set to false during local dev.
SQLX_OFFLINE=false

# ---- Email notifications ----
# Gmail SMTP — requires a Gmail App Password (not your regular password).
# Setup: Google Account → Security → 2-Step Verification → App passwords
# Leave all four unset to disable email notifications (server still works).
SMTP_FROM=you@gmail.com
SMTP_TO=you@gmail.com
SMTP_USERNAME=you@gmail.com
SMTP_PASSWORD=xxxx xxxx xxxx xxxx

# ---- Frontend (baked in at Trunk compile time via option_env!) ----
# Only needed when running the frontend dev server (trunk serve) separately
# from the backend. Leave unset in production (same-origin fetch).
# API_BASE_URL=http://localhost:8080
